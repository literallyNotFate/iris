# Theme: {{ theme_name }}, generated by Iris

set -g status on
set -g status-position bottom
set -g status-style "bg={{ bg }},fg={{ fg }}"
set -g status-justify absolute-centre
set -g status-left-length 100
set -g status-right-length 100

set -g status-left ""
set -ga status-left "#[bg={{ bg }},fg={{ green }},bold]  #S  "
set -ga status-left "#[bg={{ bg }},fg={{ comment }},none]│"

{% raw %}
set -ga status-left "#[bg={% endraw %}{{ bg }}{% raw %},fg={% endraw %}{{ operator }}{% raw %}]   #{=/-32/...:#{s|$USER|~|:#{b:pane_current_path}}} "
set -ga status-left "#[bg={% endraw %}{{ bg }}{% raw %},fg={% endraw %}{{ comment }}{% raw %},none]#{?window_zoomed_flag,│,}"
set -ga status-left "#[bg={% endraw %}{{ bg }}{% raw %},fg={% endraw %}{{ gutter_fg }}{% raw %}]#{?window_zoomed_flag,  zoom ,}"

set -g status-right ""
set -g status-right "#[fg={% endraw %}{{ blue }}{% raw %},bg={% endraw %}{{ bg }}{% raw %}]#{battery_icon} #{battery_percentage} "
set -ga status-right "#[fg={% endraw %}{{ comment }}{% raw %},bg={% endraw %}{{ bg }}{% raw %}]│"
set -ga status-right "#[bg={% endraw %}{{ bg }}{% raw %}]#{?#{==:#{online_status},on},#[fg={% endraw %}{{ func }}{% raw %}] 󰖩 on ,#[fg={% endraw %}{{ tag }}{% raw %}] 󰖪 off }"

set -wg automatic-rename on
set -g automatic-rename-format "#{pane_current_command}"
{% endraw %}

set -g window-status-format " #I:#W "
set -g window-status-style "bg={{ bg }},fg={{ gutter_fg }}"
set -g window-status-last-style "bg={{ bg }},fg={{ gutter_fg }}"
set -g window-status-activity-style "bg={{ yellow }},fg={{ bg }}"
set -g window-status-bell-style "bg={{ keyword }},fg={{ bg }},bold"
set -g window-status-separator "#[fg={{ comment }}]|"

set -g window-status-current-format " #I:#W "
set -g window-status-current-style "bg={{ yellow }},fg={{ bg }},bold"

set -g window-style "bg={{ bg }}"
set -g window-active-style "bg={{ bg }}"
set -g pane-border-style "fg={{ line_hl }}"
set -g pane-active-border-style "fg={{ yellow }},bold"

set -gq message-style "bg={{ line_hl }},fg={{ fg }}"
set -gq message-command-style "bg={{ line_hl }},fg={{ fg }}"
