# Theme: {{ theme_name }}, generated by Iris

theme[main_bg]="{{ bg }}"
theme[main_fg]="{{ fg }}"
theme[title]="{{ fg }}"
theme[hi_fg]="{{ keyword }}"
theme[selected_bg]="{{ sel }}"
theme[selected_fg]="{{ white }}"
theme[inactive_fg]="{{ comment }}"

theme[cpu_box]="{{ ansi.2 }}"
theme[cpu_text]="{{ fg }}"
theme[cpu_start]="{{ ansi.2 }}"
theme[cpu_mid]="{{ ansi.3 }}"
theme[cpu_end]="{{ ansi.9 }}"

theme[mem_box]="{{ type_name }}"
theme[mem_text]="{{ fg }}"
theme[mem_start]="{{ type_name }}"
theme[mem_mid]="{{ func }}"
theme[mem_end]="{{ tag }}"

theme[net_box]="{{ string }}"
theme[net_text]="{{ fg }}"
theme[net_start]="{{ string }}"
theme[net_mid]="{{ constant }}"
theme[net_end]="{{ attribute }}"

theme[div_line]="{{ line_hl }}"
theme[meter_bg]="{{ line_hl }}"
theme[temp_start]="{{ ansi.2 }}"
theme[temp_mid]="{{ ansi.3 }}"
theme[temp_end]="{{ ansi.9 }}"
